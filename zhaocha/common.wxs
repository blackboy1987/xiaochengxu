var fn = ({
    baseurl: (function(siteroot) {
        var arr = siteroot.split("/");
        var host_url = arr[(0)] + "//" + arr[(2)];
        return (host_url)
    }),
});
module.exports = ({
    baseurl: fn.baseurl,
});