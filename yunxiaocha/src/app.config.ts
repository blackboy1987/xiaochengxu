import { AppConfig } from "remax/wechat";

const config: AppConfig = {
  pages: ['pages/index/index'],
  window: {
    navigationBarTitleText: '宏顺耳道云小茶',
    navigationBarBackgroundColor: '#ffffff',
    navigationBarTextStyle:"black",

  }
};

export default config;
